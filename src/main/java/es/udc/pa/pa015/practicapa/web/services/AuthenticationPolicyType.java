package es.udc.pa.pa015.practicapa.web.services;

/**
 * Enumerate class authenticationPolicyType.
 */
public enum AuthenticationPolicyType {
  /** ALL USERS, AUTHENTICATED USERS, NON AUTHENTICATED USERS. */
  ALL_USERS, AUTHENTICATED_USERS, NON_AUTHENTICATED_USERS,
  /** ADMIN USER, NON ADMIN, AUTHENTICATED USERS. */
  ADMIN_USER, NON_ADMIN_AUTHENTICATED_USERS;
}
